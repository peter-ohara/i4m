<%= form_for(t_bill) do |f| %>
  <% if t_bill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(t_bill.errors.count, "error") %> prohibited this t_bill from being saved:</h2>

      <ul>
      <% t_bill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :principal %>
    <%= f.number_field :principal %>
  </div>

  <div class="field">
    <%= f.label :tenure %>
    <%= f.number_field :tenure %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
