<p id="notice"><%= notice %></p>

<h1>Saved T Bills</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Principal pesewas</th>
      <th>Tenure</th>
      <th>Bank of ghana rate</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @saved_t_bills.each do |saved_t_bill| %>
      <tr>
        <td><%= saved_t_bill.user %></td>
        <td><%= saved_t_bill.principal_pesewas %></td>
        <td><%= saved_t_bill.tenure %></td>
        <td><%= saved_t_bill.bank_of_ghana_rate %></td>
        <td><%= link_to 'Show', saved_t_bill %></td>
        <td><%= link_to 'Edit', edit_saved_t_bill_path(saved_t_bill) %></td>
        <td><%= link_to 'Destroy', saved_t_bill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Saved T Bill', new_saved_t_bill_path %>
